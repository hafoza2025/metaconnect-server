<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <title>الدعم الفني</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
</head>

<body class="bg-light">
    <!-- Navbar للإشعارات والعودة -->
    <nav class="navbar navbar-light bg-white shadow-sm mb-4">
        <div class="container">
            <span class="navbar-brand mb-0 h1">مركز الدعم</span>
            <div class="d-flex align-items-center">
                <!-- جرس الإشعارات -->
                <div class="position-relative me-4" style="cursor: pointer;">
                    <span style="font-size: 1.5rem;">🔔</span>
                    <span id="notifBadge"
                        class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"
                        style="display:none;">
                        0
                    </span>
                </div>
                <a href="/dev-dashboard" class="btn btn-outline-secondary btn-sm">عودة للرئيسية</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3>💬 تذاكر الدعم الفني</h3>
            <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#newTicketModal">+ تذكرة
                جديدة</button>
        </div>

        <div class="list-group shadow-sm">
            <% if(tickets.length==0) { %>
                <div class="text-center p-5 text-muted">لا توجد تذاكر حالياً</div>
                <% } %>
                    <% tickets.forEach(function(ticket) { %>
                        <a href="/dev/support/view/<%= ticket.id %>"
                            class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                            <div>
                                <div class="d-flex align-items-center">
                                    <h5 class="mb-1 me-2">
                                        <%= ticket.subject %>
                                    </h5>
                                    <% if(ticket.company_name) { %>
                                        <span class="badge bg-info text-dark me-2" style="font-size: 0.7em">
                                            <%= ticket.company_name %>
                                        </span>
                                        <% } %>
                                </div>
                                <small class="text-muted">تم الفتح: <%= ticket.created_at.toLocaleString() %></small>
                            </div>
                            <div>
                                <span
                                    class="badge bg-<%= ticket.status == 'open' ? 'success' : (ticket.status == 'closed' ? 'secondary' : 'warning') %>">
                                    <%= ticket.status=='open' ? 'مفتوحة' : (ticket.status=='closed' ? 'مغلقة' : 'معلقة'
                                        ) %>
                                </span>
                            </div>
                        </a>
                        <% }); %>
        </div>
    </div>

    <!-- Modal تذكرة جديدة -->
    <div class="modal fade" id="newTicketModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <form action="/dev/support/new" method="POST">
                    <div class="modal-header">
                        <h5 class="modal-title">فتح تذكرة دعم جديدة</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <!-- اختيار الشركة (مهم جداً) -->
                        <div class="mb-3">
                            <label class="form-label fw-bold">الشركة المتعلقة بالمشكلة</label>
                            <select name="company_id" class="form-select" required>
                                <option value="" disabled selected>-- اختر الشركة --</option>
                                <% companies.forEach(function(comp) { %>
                                    <option value="<%= comp.id %>">
                                        <%= comp.name %> (<%= comp.country_code=='EG' ? 'مصر' : 'السعودية' %>)
                                    </option>
                                    <% }); %>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">نوع الطلب</label>
                            <select name="subject" class="form-select">
                                <option value="طلب شحن رصيد">💰 طلب شحن رصيد</option>
                                <option value="مشكلة تقنية في الربط">🔧 مشكلة تقنية في الربط</option>
                                <option value="استفسار عام">❓ استفسار عام</option>
                                <option value="مشكلة في الضرائب">🏛 مشكلة في الضرائب</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">تفاصيل الرسالة</label>
                            <textarea name="message" class="form-control" rows="4" placeholder="اشرح مشكلتك بالتفصيل..."
                                required></textarea>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
                        <button type="submit" class="btn btn-primary">إنشاء التذكرة</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // سكربت الإشعارات
        function checkNotifications() {
            fetch('/api/notifications/count')
                .then(res => res.json())
                .then(data => {
                    const badge = document.getElementById('notifBadge');
                    if (data.count > 0) {
                        badge.innerText = data.count;
                        badge.style.display = 'inline-block';
                    } else {
                        badge.style.display = 'none';
                    }
                });
        }
        setInterval(checkNotifications, 10000);
        checkNotifications();
    </script>
</body>

</html>