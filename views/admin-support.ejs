<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <title>إدارة الدعم الفني</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
</head>

<body class="bg-light">
    <div class="container mt-5">
        <h3>📥 التذاكر الواردة</h3>
        <div class="list-group mt-4">
            <% tickets.forEach(function(ticket) { %>
                <a href="/admin/support/view/<%= ticket.id %>" class="list-group-item list-group-item-action">
                    <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1">
                            <%= ticket.subject %>
                        </h5>
                        <small>
                            <%= ticket.created_at.toLocaleString() %>
                        </small>
                    </div>
                    <p class="mb-1">العميل: <%= ticket.dev_name %>
                    </p>
                    <span class="badge bg-<%= ticket.status == 'open' ? 'danger' : 'secondary' %>">
                        <%= ticket.status=='open' ? 'يحتاج رد' : 'مغلق' %>
                    </span>
                </a>
                <% }); %>
        </div>
        <a href="/admin-dashboard" class="btn btn-secondary mt-3">عودة للرئيسية</a>
    </div>

    <!-- في النافبار Navbar بالأعلى، أضف أيقونة الجرس -->
    <li class="nav-item position-relative me-3">
        <a href="<%= userType === 'admin' ? '/admin/support' : '/dev/support' %>" class="nav-link text-white">
            🔔
            <span id="notifBadge"
                class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger"
                style="display:none;">
                0
            </span>
        </a>
    </li>

    <script>
        // فحص الإشعارات كل 10 ثواني
        function checkNotifications() {
            fetch('/api/notifications/count')
                .then(res => res.json())
                .then(data => {
                    const badge = document.getElementById('notifBadge');
                    if (data.count > 0) {
                        badge.innerText = data.count;
                        badge.style.display = 'inline-block';
                        // تشغيل صوت (اختياري)
                        // new Audio('/notification.mp3').play(); 
                    } else {
                        badge.style.display = 'none';
                    }
                });
        }
        setInterval(checkNotifications, 10000); // كل 10 ثواني
        checkNotifications(); // تشغيل فوراً عند التحميل
    </script>

</body>

</html>