<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة القيادة المركزية | MetaConnect</title>
    
    <!-- CSS Libraries -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #34495e;
            --accent-color: #3498db;
            --success-color: #2ecc71;
            --danger-color: #e74c3c;
            --light-bg: #f4f6f9;
            --sidebar-width: 260px;
        }

        body {
            font-family: 'Cairo', sans-serif;
            background-color: var(--light-bg);
            overflow-x: hidden;
        }

        /* Sidebar Styles */
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            background: var(--primary-color);
            color: #fff;
            position: fixed;
            top: 0;
            right: 0;
            padding-top: 20px;
            z-index: 1000;
            box-shadow: -4px 0 15px rgba(0,0,0,0.1);
        }

        .sidebar-header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            margin-bottom: 20px;
        }

        .sidebar-header h3 {
            font-weight: 800;
            letter-spacing: 1px;
        }
        .sidebar-header span { color: var(--accent-color); }

        .nav-link {
            color: #ecf0f1;
            padding: 12px 20px;
            margin: 5px 15px;
            border-radius: 8px;
            transition: 0.3s;
            font-size: 1rem;
        }

        .nav-link:hover, .nav-link.active {
            background: var(--secondary-color);
            color: var(--accent-color);
            transform: translateX(-5px);
        }

        .nav-link i { margin-left: 10px; width: 20px; text-align: center; }

        /* Main Content */
        .main-content {
            margin-right: var(--sidebar-width);
            padding: 30px;
            transition: 0.3s;
        }

        /* Stats Cards */
        .stat-card {
            background: #fff;
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            border-right: 4px solid transparent;
            transition: transform 0.3s;
            position: relative;
            overflow: hidden;
        }

        .stat-card:hover { transform: translateY(-5px); }
        .stat-card.blue { border-color: var(--accent-color); }
        .stat-card.green { border-color: var(--success-color); }
        .stat-card.red { border-color: var(--danger-color); }
        .stat-card.dark { border-color: var(--primary-color); }

        .stat-icon {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 3rem;
            opacity: 0.1;
        }

        .stat-title { font-size: 0.9rem; color: #7f8c8d; font-weight: 600; }
        .stat-value { font-size: 2rem; font-weight: 800; color: #2c3e50; margin: 5px 0; }
        .stat-trend { font-size: 0.8rem; }

        /* Tables */
        .custom-table-card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            overflow: hidden;
        }
        
        .table-header {
            padding: 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .table thead th {
            background: #f8f9fa;
            border: none;
            color: #7f8c8d;
            font-weight: 600;
            padding: 15px;
        }
        
        .table tbody td { padding: 15px; vertical-align: middle; }

        /* Flags & Badges */
        .flag-icon { width: 24px; height: 24px; border-radius: 50%; object-fit: cover; margin-left: 5px; }
        .status-badge {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 700;
        }
        .bg-sa { background-color: #d4edda; color: #155724; }
        .bg-eg { background-color: #fff3cd; color: #856404; }

        /* Live Indicator */
        .live-dot {
            width: 10px; height: 10px;
            background-color: #e74c3c;
            border-radius: 50%;
            display: inline-block;
            animation: blink 1.5s infinite;
            margin-left: 5px;
        }
        @keyframes blink { 50% { opacity: 0; } }

    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <div class="sidebar-header">
            <h3>Meta<span>Connect</span></h3>
            <small class="text-white-50">لوحة القيادة المركزية</small>
        </div>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link active" href="#"><i class="fas fa-tachometer-alt"></i> نظرة عامة</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-building"></i> إدارة الشركات</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-code"></i> المطورين</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/admin/support"><i class="fas fa-headset"></i> الدعم الفني <span class="badge bg-danger ms-2" id="sidebarTicketCount">0</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-file-invoice-dollar"></i> الفواتير والعمليات</a>
            </li>
            <li class="nav-item mt-4">
                <a class="nav-link text-danger" href="/logout"><i class="fas fa-sign-out-alt"></i> تسجيل خروج</a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        
        <!-- Top Header -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h4 class="fw-bold mb-1">مرحباً، المدير العام 👋</h4>
                <p class="text-muted mb-0"><span class="live-dot"></span> النظام يعمل بشكل مباشر</p>
            </div>
            <div class="d-flex gap-2">
                <button class="btn btn-outline-primary" onclick="fetchData()"><i class="fas fa-sync-alt"></i> تحديث</button>
                <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addCompanyModal"><i class="fas fa-plus"></i> شركة جديدة</button>
            </div>
        </div>

        <!-- Stats Row -->
        <div class="row g-4 mb-4">
            <!-- Total Invoices -->
            <div class="col-md-3">
                <div class="stat-card blue">
                    <i class="fas fa-receipt stat-icon"></i>
                    <div class="stat-title">إجمالي الفواتير</div>
                    <div class="stat-value" id="statTotalInvoices">0</div>
                    <div class="stat-trend text-success"><i class="fas fa-arrow-up"></i> <span id="statTodayInvoices">0</span> اليوم</div>
                </div>
            </div>
            <!-- Total Companies -->
            <div class="col-md-3">
                <div class="stat-card dark">
                    <i class="fas fa-building stat-icon"></i>
                    <div class="stat-title">الشركات النشطة</div>
                    <div class="stat-value" id="statTotalCompanies">0</div>
                    <div class="stat-trend">
                        <span class="badge bg-sa ms-1" title="السعودية">🇸🇦 <span id="countSA">0</span></span>
                        <span class="badge bg-eg" title="مصر">🇪🇬 <span id="countEG">0</span></span>
                    </div>
                </div>
            </div>
            <!-- Developers -->
            <div class="col-md-3">
                <div class="stat-card green">
                    <i class="fas fa-code stat-icon"></i>
                    <div class="stat-title">شركاء التطوير</div>
                    <div class="stat-value" id="statTotalDevs">0</div>
                    <div class="stat-trend text-muted">مطورين مسجلين</div>
                </div>
            </div>
            <!-- Failed Invoices -->
            <div class="col-md-3">
                <div class="stat-card red">
                    <i class="fas fa-exclamation-triangle stat-icon"></i>
                    <div class="stat-title">عمليات فاشلة</div>
                    <div class="stat-value text-danger" id="statFailedInvoices">0</div>
                    <div class="stat-trend text-muted">تحتاج مراجعة</div>
                </div>
            </div>
        </div>

        <div class="row">
            <!-- Companies Table -->
            <div class="col-lg-8">
                <div class="custom-table-card">
                    <div class="table-header">
                        <h5 class="mb-0 fw-bold">📋 أحدث الشركات المسجلة</h5>
                        <input type="text" class="form-control form-control-sm w-auto" placeholder="بحث سريع..." onkeyup="searchTable()">
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover" id="companiesTable">
                            <thead>
                                <tr>
                                    <th>الشركة</th>
                                    <th>الدولة</th>
                                    <th>الرقم الضريبي</th>
                                    <th>تاريخ الانضمام</th>
                                    <th>إجراءات</th>
                                </tr>
                            </thead>
                            <tbody id="companiesList">
                                <!-- سيتم ملء البيانات هنا بواسطة JavaScript -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            <!-- Recent Developers & Quick Actions -->
            <div class="col-lg-4">
                <div class="custom-table-card mb-4">
                    <div class="table-header border-bottom-0 pb-0">
                        <h5 class="mb-0 fw-bold">👨‍💻 أحدث المطورين</h5>
                    </div>
                    <div class="p-3" id="devsList">
                        <!-- قائمة المطورين -->
                    </div>
                </div>

                <div class="card border-0 shadow-sm p-3">
                    <h6 class="fw-bold mb-3">⚙️ تحكم سريع</h6>
                    <div class="d-grid gap-2">
                        <button class="btn btn-outline-dark btn-sm text-start"><i class="fas fa-pause-circle ms-2"></i> إيقاف النظام مؤقتاً للصيانة</button>
                        <button class="btn btn-outline-info btn-sm text-start"><i class="fas fa-database ms-2"></i> أخذ نسخة احتياطية (Backup)</button>
                    </div>
                </div>
            </div>
        </div>

    </div>

    <!-- Modal إضافة شركة -->
    <div class="modal fade" id="addCompanyModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <form action="/add-company" method="POST">
                    <div class="modal-header">
                        <h5 class="modal-title">إضافة منشأة جديدة</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                    </div>
                    <div class="modal-body">
                        <div class="mb-3">
                            <label>اسم المنشأة</label>
                            <input type="text" name="name" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label>الرقم الضريبي</label>
                            <input type="text" name="tax_id" class="form-control" required>
                        </div>
                        <div class="mb-3">
                            <label>النطاق الضريبي (الدولة)</label>
                            <select name="country_code" class="form-select">
                                <option value="SA">🇸🇦 المملكة العربية السعودية (ZATCA)</option>
                                <option value="EG">🇪🇬 جمهورية مصر العربية (ETA)</option>
                            </select>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إلغاء</button>
                        <button type="submit" class="btn btn-primary">تسجيل المنشأة</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- JavaScript للتحكم اللحظي -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        
        // دالة لجلب البيانات لحظياً
        async function fetchData() {
            try {
                const response = await fetch('/api/admin/dashboard-stats');
                const data = await response.json();

                if (data.stats) {
                    // تحديث الأرقام
                    animateValue("statTotalInvoices", data.stats.invoices.total);
                    document.getElementById("statTodayInvoices").innerText = data.stats.invoices.today;
                    animateValue("statTotalCompanies", data.stats.totalCompanies);
                    document.getElementById("countSA").innerText = data.stats.saudiCount;
                    document.getElementById("countEG").innerText = data.stats.egyptCount;
                    animateValue("statTotalDevs", data.stats.totalDevs);
                    document.getElementById("statFailedInvoices").innerText = data.stats.invoices.failed;
                }

                // تحديث جدول الشركات
                const tbody = document.getElementById('companiesList');
                tbody.innerHTML = '';
                data.recentCompanies.forEach(comp => {
                    const flag = comp.country_code === 'EG' ? '🇪🇬 مصر' : '🇸🇦 السعودية';
                    const date = new Date(comp.created_at).toLocaleDateString('ar-EG');
                    
                    tbody.innerHTML += `
                        <tr>
                            <td class="fw-bold text-dark">${comp.name}</td>
                            <td><span class="badge ${comp.country_code === 'EG' ? 'bg-warning text-dark' : 'bg-success'}">${flag}</span></td>
                            <td class="text-muted" style="font-family:monospace">${comp.tax_id}</td>
                            <td>${date}</td>
                            <td>
                                <button class="btn btn-sm btn-light text-primary" title="تفاصيل"><i class="fas fa-eye"></i></button>
                                <button class="btn btn-sm btn-light text-danger" onclick="deleteCompany(${comp.id})" title="حذف"><i class="fas fa-trash"></i></button>
                            </td>
                        </tr>
                    `;
                });

                // تحديث قائمة المطورين
                const devList = document.getElementById('devsList');
                devList.innerHTML = '';
                data.recentDevs.forEach(dev => {
                    devList.innerHTML += `
                        <div class="d-flex align-items-center mb-3 pb-3 border-bottom">
                            <div class="bg-light rounded-circle d-flex align-items-center justify-content-center text-primary fw-bold" style="width:40px; height:40px;">
                                ${dev.name.charAt(0)}
                            </div>
                            <div class="ms-3">
                                <h6 class="mb-0 fw-bold">${dev.name}</h6>
                                <small class="text-muted">${dev.email}</small>
                            </div>
                        </div>
                    `;
                });

            } catch (err) {
                console.error("Error fetching live data:", err);
            }
        }

        // دالة لحذف شركة (تفاعل حقيقي)
        async function deleteCompany(id) {
            if(confirm('هل أنت متأكد من حذف هذه الشركة وجميع بياناتها؟')) {
                await fetch('/api/admin/delete-company', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({id})
                });
                fetchData(); // تحديث البيانات فوراً
            }
        }

        // تأثير عداد الأرقام
        function animateValue(id, end) {
            const obj = document.getElementById(id);
            const start = parseInt(obj.innerText) || 0;
            if (start === end) return;
            obj.innerText = end;
            obj.style.color = '#2ecc71'; // وميض أخضر عند التحديث
            setTimeout(() => obj.style.color = '', 500);
        }

        // تشغيل التحديث التلقائي كل 5 ثواني
        setInterval(fetchData, 5000);
        fetchData(); // التشغيل الأول

    </script>
</body>
</html>
