<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>Nerve Center | MetaConnect</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800&display=swap" rel="stylesheet">
    
    <style>
        body { font-family: 'Tajawal', sans-serif; background: #f0f2f5; overflow-x: hidden; }
        
        /* Sidebar */
        .sidebar { width: 260px; height: 100vh; background: #111827; color: white; position: fixed; top: 0; right: 0; padding: 25px 15px; z-index: 1000; }
        .sidebar h4 { font-weight: 800; margin-bottom: 50px; letter-spacing: 1px; color: #fff; text-align: center; }
        .sidebar h4 span { color: #3b82f6; }
        .nav-link { color: #9ca3af; padding: 14px 15px; border-radius: 10px; transition: 0.3s; margin-bottom: 8px; display: flex; align-items: center; cursor: pointer; font-weight: 500; }
        .nav-link i { width: 30px; font-size: 1.2rem; margin-left: 10px; }
        .nav-link:hover, .nav-link.active { background: #1f2937; color: white; transform: translateX(-5px); }
        
        /* Main Content */
        .main-content { margin-right: 260px; padding: 40px; }
        
        /* Sections Logic */
        .content-section { display: none; animation: fadeIn 0.4s; }
        .content-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        /* Cards */
        .stat-card { background: white; padding: 30px; border-radius: 16px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); position: relative; overflow: hidden; transition: transform 0.3s; border: 1px solid #e5e7eb; }
        .stat-card:hover { transform: translateY(-5px); box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1); }
        .stat-icon { position: absolute; left: 25px; top: 50%; transform: translateY(-50%); font-size: 3.5rem; opacity: 0.1; color: #111827; }
        
        /* Tables */
        .table-container { background: white; border-radius: 16px; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); padding: 25px; border: 1px solid #e5e7eb; }
        .table thead th { background: #f9fafb; border-bottom: 2px solid #e5e7eb; color: #4b5563; font-weight: 700; padding: 15px; font-size: 0.9rem; text-transform: uppercase; letter-spacing: 0.5px; }
        .table tbody td { vertical-align: middle; padding: 16px 15px; color: #1f2937; }
        .table tbody tr { transition: background 0.2s; cursor: pointer; }
        .table tbody tr:hover { background-color: #f3f4f6; }

        /* Modal Styles (الملفات) */
        .modal-content { border: none; border-radius: 20px; box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25); }
        .modal-header { background: #1f2937; color: white; border-bottom: none; padding: 25px 30px; }
        .modal-body { background: #f9fafb; padding: 0; }
        
        .profile-header { background: white; padding: 30px; border-bottom: 1px solid #e5e7eb; display: flex; align-items: center; }
        .profile-avatar { width: 80px; height: 80px; background: #e5e7eb; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 2rem; font-weight: bold; color: #4b5563; margin-left: 20px; }
        .profile-info h3 { font-weight: 800; color: #111827; margin-bottom: 5px; }
        .profile-info p { color: #6b7280; margin-bottom: 0; }

        .profile-content { padding: 30px; }
        .info-box { background: white; border-radius: 12px; padding: 20px; border: 1px solid #e5e7eb; margin-bottom: 20px; }
        .info-box h6 { font-weight: 700; color: #374151; margin-bottom: 15px; border-bottom: 1px solid #f3f4f6; padding-bottom: 10px; }
        .info-item { display: flex; justify-content: space-between; margin-bottom: 10px; font-size: 0.95rem; }
        .info-label { color: #6b7280; }
        .info-value { font-weight: 600; color: #111827; }

    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <h4>Meta<span>Connect</span></h4>
        <nav class="nav flex-column">
            <a class="nav-link active" onclick="switchSection('overview', this)"><i class="fas fa-chart-pie"></i> نظرة عامة</a>
            <a class="nav-link" onclick="switchSection('companies', this)"><i class="fas fa-building"></i> ملفات الشركات</a>
            <a class="nav-link" onclick="switchSection('developers', this)"><i class="fas fa-users-cog"></i> المطورين</a>
            <a class="nav-link" href="/admin/support"><i class="fas fa-headset"></i> الدعم الفني</a>
            <div style="margin-top: auto; padding-top: 50px;">
                <a class="nav-link text-danger" href="/logout"><i class="fas fa-power-off"></i> تسجيل خروج</a>
            </div>
        </nav>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        
        <div class="d-flex justify-content-between align-items-center mb-5">
            <div>
                <h2 class="fw-bold text-dark mb-1" id="pageTitle">لوحة التحكم المركزية</h2>
                <p class="text-muted mb-0">إدارة النظام والعمليات لحظياً</p>
            </div>
            <div class="d-flex align-items-center gap-3">
                <div class="bg-white px-4 py-2 rounded-pill shadow-sm border d-flex align-items-center gap-2">
                    <div style="width:10px; height:10px; background:#10b981; border-radius:50%;"></div>
                    <span class="fw-bold small text-dark">System Online</span>
                </div>
                <button class="btn btn-dark rounded-pill px-4" onclick="fetchDashboardData()"><i class="fas fa-sync-alt me-2"></i> تحديث</button>
            </div>
        </div>

        <!-- SECTION 1: Overview -->
        <div id="overview" class="content-section active">
            <div class="row g-4 mb-5">
                <div class="col-md-4">
                    <div class="stat-card">
                        <i class="fas fa-file-invoice-dollar stat-icon"></i>
                        <h6 class="text-muted text-uppercase small fw-bold">إجمالي الفواتير</h6>
                        <h2 class="fw-bold mb-0 display-5" id="statTotalInvoices">...</h2>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="stat-card">
                        <i class="fas fa-building stat-icon"></i>
                        <h6 class="text-muted text-uppercase small fw-bold">الشركات المسجلة</h6>
                        <h2 class="fw-bold mb-0 display-5" id="statTotalCompanies">...</h2>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="stat-card">
                        <i class="fas fa-users stat-icon"></i>
                        <h6 class="text-muted text-uppercase small fw-bold">المطورين</h6>
                        <h2 class="fw-bold mb-0 display-5" id="statTotalDevs">...</h2>
                    </div>
                </div>
            </div>
            
            <div class="table-container">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">🚀 أحدث الشركات المنضمة</h5>
                    <button class="btn btn-sm btn-outline-dark" onclick="switchSection('companies')">عرض الكل</button>
                </div>
                <table class="table">
                    <thead><tr><th>الشركة</th><th>الدولة</th><th>المطور</th><th>الحالة</th></tr></thead>
                    <tbody id="recentActivity"></tbody>
                </table>
            </div>
        </div>

        <!-- SECTION 2: Companies Archive -->
        <div id="companies" class="content-section">
            <div class="table-container">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">📂 أرشيف الشركات الكامل</h5>
                    <input type="text" class="form-control w-25 rounded-pill border-2 px-3" placeholder="بحث عن شركة..." onkeyup="filterCompanies(this.value)">
                </div>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>اسم الشركة</th>
                                <th>الدولة</th>
                                <th>الرقم الضريبي</th>
                                <th>الحالة</th>
                                <th>إجراء</th>
                            </tr>
                        </thead>
                        <tbody id="companiesListFull"></tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- SECTION 3: Developers -->
        <div id="developers" class="content-section">
            <div class="table-container">
                <h5 class="fw-bold mb-4">👨‍💻 المطورين وشركات البرمجة</h5>
                <div class="row g-4" id="devsGrid"></div>
            </div>
        </div>

    </div>

    <!-- Developer File Modal (الملف الكامل للمطور) -->
    <div class="modal fade" id="devFileModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">ملف المطور الشامل</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" id="devModalContent">
                    <div class="text-center py-5"><i class="fas fa-spinner fa-spin fa-3x text-muted"></i></div>
                </div>
            </div>
        </div>
    </div>

    <!-- Company File Modal (الملف الكامل للشركة) -->
    <div class="modal fade" id="companyFileModal" tabindex="-1">
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">ملف الشركة الرقمي</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body" id="compModalContent">
                    <div class="text-center py-5"><i class="fas fa-spinner fa-spin fa-3x text-muted"></i></div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        
        // Navigation
        function switchSection(sectionId, linkElement) {
            document.querySelectorAll('.content-section').forEach(el => el.classList.remove('active'));
            document.getElementById(sectionId).classList.add('active');
            if(linkElement) {
                document.querySelectorAll('.nav-link').forEach(el => el.classList.remove('active'));
                linkElement.classList.add('active');
            }
            const titles = {'overview': 'لوحة التحكم المركزية', 'companies': 'أرشيف الشركات', 'developers': 'إدارة المطورين'};
            document.getElementById('pageTitle').innerText = titles[sectionId] || 'MetaConnect';
        }

        // Fetch Data
        async function fetchDashboardData() {
            try {
                const res = await fetch('/api/admin/dashboard-stats');
                const data = await res.json();

                // Update Stats
                document.getElementById('statTotalInvoices').innerText = data.stats.invoices.total;
                document.getElementById('statTotalCompanies').innerText = data.stats.totalCompanies;
                document.getElementById('statTotalDevs').innerText = data.stats.totalDevs;

                // Update Companies Table
                const tbody = document.getElementById('companiesListFull');
                tbody.innerHTML = '';
                // Assuming recentCompanies contains all needed data for demo
                data.recentCompanies.forEach(c => { 
                    const flag = c.country_code === 'EG' ? '🇪🇬' : '🇸🇦';
                    tbody.innerHTML += `
                        <tr onclick="openCompanyFile(${c.id})">
                            <td class="text-muted fw-bold">#${c.id}</td>
                            <td class="fw-bold text-dark">${c.name}</td>
                            <td><span class="badge bg-light text-dark border">${flag} ${c.country_code}</span></td>
                            <td class="font-monospace text-muted">${c.tax_id}</td>
                            <td><span class="badge bg-green-100 text-green-800" style="background:#dcfce7; color:#166534">نشط</span></td>
                            <td><button class="btn btn-sm btn-dark px-3 rounded-pill">عرض الملف</button></td>
                        </tr>
                    `;
                });

                // Update Recent Activity (Overview)
                const recentBody = document.getElementById('recentActivity');
                recentBody.innerHTML = '';
                data.recentCompanies.slice(0, 5).forEach(c => {
                    recentBody.innerHTML += `
                        <tr onclick="switchSection('companies')">
                            <td class="fw-bold">${c.name}</td>
                            <td>${c.country_code === 'EG' ? '🇪🇬' : '🇸🇦'}</td>
                            <td class="text-muted">System</td>
                            <td><span class="badge bg-light text-dark border">انضم حديثاً</span></td>
                        </tr>
                    `;
                });

                // Update Developers Grid
                const devGrid = document.getElementById('devsGrid');
                devGrid.innerHTML = '';
                data.recentDevs.forEach(d => {
                    devGrid.innerHTML += `
                        <div class="col-md-4">
                            <div class="stat-card p-4 cursor-pointer" onclick="openDevFile(${d.id})" style="cursor:pointer">
                                <div class="d-flex align-items-center mb-3">
                                    <div class="bg-light rounded-circle d-flex align-items-center justify-content-center fw-bold text-primary" style="width:50px; height:50px; font-size:1.2rem">
                                        ${d.name.charAt(0).toUpperCase()}
                                    </div>
                                    <div class="ms-3">
                                        <h6 class="fw-bold mb-0">${d.name}</h6>
                                        <small class="text-muted">${d.email}</small>
                                    </div>
                                </div>
                                <div class="d-flex justify-content-between align-items-center pt-3 border-top">
                                    <span class="text-muted small">الرصيد</span>
                                    <span class="fw-bold text-success">$${d.wallet_balance}</span>
                                </div>
                            </div>
                        </div>
                    `;
                });

            } catch (e) { console.error(e); }
        }

        // OPEN DEVELOPER FILE (FULL DETAILS)
        async function openDevFile(id) {
            const modal = new bootstrap.Modal(document.getElementById('devFileModal'));
            modal.show();
            const content = document.getElementById('devModalContent');
            
            try {
                const res = await fetch(`/api/admin/developer-file/${id}`);
                const data = await res.json();
                const dev = data.profile;
                
                content.innerHTML = `
                    <div class="profile-header">
                        <div class="profile-avatar">${dev.name.charAt(0)}</div>
                        <div class="profile-info ms-3">
                            <h3>${dev.name}</h3>
                            <p>${dev.email} | ${dev.phone || 'No Phone'}</p>
                            <span class="badge bg-primary mt-2">Developer Partner</span>
                        </div>
                        <div class="ms-auto text-end">
                            <h2 class="fw-bold text-success mb-0">$${dev.wallet_balance}</h2>
                            <small class="text-muted">رصيد المحفظة</small>
                        </div>
                    </div>
                    <div class="profile-content bg-light">
                        <div class="row">
                            <div class="col-md-4">
                                <div class="info-box">
                                    <h6>معلومات التواصل</h6>
                                    <div class="info-item"><span class="info-label">الدولة:</span> <span class="info-value">${dev.country || '-'}</span></div>
                                    <div class="info-item"><span class="info-label">المسؤول:</span> <span class="info-value">${dev.contact_person || '-'}</span></div>
                                    <div class="info-item"><span class="info-label">الموقع:</span> <span class="info-value"><a href="${dev.website}" target="_blank">زيارة</a></span></div>
                                </div>
                            </div>
                            <div class="col-md-8">
                                <div class="info-box">
                                    <h6>الشركات التابعة (${data.companies.length})</h6>
                                    <table class="table table-sm mb-0">
                                        <thead><tr><th>الشركة</th><th>الدولة</th><th>الرقم الضريبي</th></tr></thead>
                                        <tbody>
                                            ${data.companies.length > 0 ? data.companies.map(c => `
                                                <tr>
                                                    <td class="fw-bold text-primary">${c.name}</td>
                                                    <td>${c.country_code}</td>
                                                    <td class="font-monospace">${c.tax_id}</td>
                                                </tr>
                                            `).join('') : '<tr><td colspan="3" class="text-center text-muted">لا توجد شركات مسجلة</td></tr>'}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            } catch (e) { content.innerHTML = '<div class="alert alert-danger m-3">Error loading file</div>'; }
        }

        // OPEN COMPANY FILE (FULL DETAILS)
        async function openCompanyFile(id) {
            const modal = new bootstrap.Modal(document.getElementById('companyFileModal'));
            modal.show();
            const content = document.getElementById('compModalContent');

            try {
                const res = await fetch(`/api/admin/company-file/${id}`);
                const data = await res.json();
                const info = data.info;
                const creds = data.credentials;

                content.innerHTML = `
                    <div class="profile-header">
                        <div class="profile-avatar"><i class="fas fa-building"></i></div>
                        <div class="profile-info ms-3">
                            <h3>${info.name}</h3>
                            <p>TAX ID: ${info.tax_id}</p>
                            <span class="badge bg-dark mt-2">${info.country_code === 'EG' ? 'ETA Egypt' : 'ZATCA Saudi'}</span>
                        </div>
                    </div>
                    <div class="profile-content bg-light">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="info-box">
                                    <h6>بيانات الربط (Credentials)</h6>
                                    <div class="bg-dark rounded p-3 font-monospace text-info mb-3" style="font-size:0.85rem; word-break:break-all;">
                                        ${info.country_code === 'EG' ? 
                                            `Client ID: <span class="text-white">${creds.id || '-'}</span><br>Secret: <span class="text-white">${creds.secret || '-'}</span>` : 
                                            `OTP Code: <span class="text-white">${creds.otp || '-'}</span>`
                                        }
                                    </div>
                                    <button class="btn btn-outline-danger btn-sm w-100"><i class="fas fa-key me-2"></i> إعادة تعيين المفاتيح</button>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-box">
                                    <h6>المطور المسؤول</h6>
                                    <div class="d-flex align-items-center mb-3">
                                        <div class="bg-light rounded-circle p-2 text-primary"><i class="fas fa-user"></i></div>
                                        <div class="ms-3">
                                            <h6 class="mb-0 border-0 pb-0">${info.dev_name || 'النظام'}</h6>
                                            <small class="text-muted">${info.dev_email || '-'}</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12">
                                <div class="info-box">
                                    <h6>سجل الفواتير والعمليات</h6>
                                    <table class="table table-sm">
                                        <thead><tr><th>ID</th><th>القيمة</th><th>الحالة</th><th>UUID</th></tr></thead>
                                        <tbody>
                                            ${data.invoices.length > 0 ? data.invoices.map(i => `
                                                <tr>
                                                    <td>${i.internal_id}</td>
                                                    <td>${i.total_amount}</td>
                                                    <td><span class="badge ${i.status==='submitted'?'bg-success':'bg-danger'}">${i.status}</span></td>
                                                    <td class="small text-muted">${i.gov_uuid || '-'}</td>
                                                </tr>
                                            `).join('') : '<tr><td colspan="4" class="text-center">لا توجد فواتير</td></tr>'}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            } catch (e) { content.innerHTML = '<div class="alert alert-danger m-3">Error loading file</div>'; }
        }

        // Init
        fetchDashboardData();
        setInterval(fetchDashboardData, 10000);
    </script>
</body>
</html>
